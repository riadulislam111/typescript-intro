const user:{name: string, age: number, isAdmin: boolean} = {
    name: 'John',
    age: 30,
    isAdmin: true
}

const {age, isAdmin} = user;


const userInfo = ['Rohomot Ali', 34];

const [ , userAge] = userInfo;